# --- Web / API ---
fastapi==0.115.6
uvicorn[standard]==0.34.0
python-multipart==0.0.20
pydantic==2.10.4
python-dotenv==1.0.1

# --- Document parsing ---
pdfplumber==0.11.5
python-docx==1.1.2

# --- Matching ---
rapidfuzz==3.11.0

# --- LLM providers (all OpenAI-compatible except anthropic) ---
# Groq (default), OpenAI, and xAI/Grok all use this one SDK via base_url.
openai==1.59.6
anthropic==0.42.0        # optional: only used if LLM_PROVIDER=anthropic

# --- Supabase (server-side reads/writes) ---
supabase==2.11.0

# --- Optional semantic matching (heavy: pulls torch). Install only if you want it. ---
# sentence-transformers==3.3.1
